<%= form_with(model: patienttest, local: true) do |form| %>
  <% if patienttest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(patienttest.errors.count, "error") %> prohibited this patienttest from being saved:</h2>

      <ul>
      <% patienttest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="ui form">
  <div class="ui required field">
    <%= form.label :"Patient ID" %>
    <%= form.number_field :patientID %>
  </div>

  <div class="ui required field">
    <%= form.label :"Test ID" %>
    <%= form.number_field :testID %>
  </div>

  <div class="ui required field">
    <%= form.label :"Timestamp" %>
    <%= form.datetime_select :dateAndTime %>
  </div>

  <div class="actions">
    <%=link_to 'Back', patienttests_path, {class: "ui button"} %>
    <button class="ui positive button" type="submit">Submit</button>
  </div>
<% end %>
</div>