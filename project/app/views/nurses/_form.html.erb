<%= form_with(model: nurse, local: true) do |form| %>
  <% if nurse.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nurse.errors.count, "error") %> prohibited this nurse from being saved:</h2>

      <ul>
      <% nurse.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class = "ui form">
  <div class="ui required field">
    <%= form.label :"ID" %>
    <%= form.text_field :nurseID %>
  </div>

    <div class="ui required field">
    <%= form.label :"Name" %>
    <%= form.text_field :nurseName %>
    </div>

    <div class="ui required field">
    <%= form.label :department %>
    <%= form.text_field :department %>
    </div>

    <%#= form.submit %>
      <div class="actions">
        <%=link_to 'Back', nurses_path, {class: "ui button"} %>
        <button class="ui positive button" type="submit">Submit</button>
      </div>
    <% end %>

</div>
